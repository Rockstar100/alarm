[project]
name = "alarm"
version = "0.1.0"
description = "A focused Python CLI alarm clock"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
